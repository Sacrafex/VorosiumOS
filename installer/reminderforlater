When working on main.c have the install script build from the github image and print out the log there. From there it will move a portable version of the working directory to a usb or whatever device is selected, otherwise the user can select simply moving the live directory where they would like. Information on how to boot from a usb can be added there also (display documentation for most motherboards).